[
  {
    "title": "Bisect",
    "data": [
      ["git bisect start @ #abcdef --", "Start a bisect where @ is bad and #abcdef is good"],
      ["git bisect run something", "Run a command for each commit in the bisect, and use the return value to determine if it's good or bad"],
      ["git bisect good; git bisect bad;", "Mark the current commit as good or bad"],
      ["git bisect reset", "Reset the bisect state"]
    ]
  }
]
